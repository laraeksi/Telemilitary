# Simple runtime config values.
# Keeps SQLite path and debug flag.
class Config:
    # SQLite database file (beginner friendly).
    DB_PATH = "data/game.db"
    # Debug mode for local dev only.
    DEBUG = True
    # CORS is configured in app.py.
    # Add more settings here if needed.
