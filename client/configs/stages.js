/**
 * Pseudocode:
 * 1) Re-export shared stage configuration.
 * 2) Replace relative import with aliases later.
 */

export { CONFIG_DEFINITIONS, STAGE_CONFIGS, getStageConfigById } from "../../shared/stages";
