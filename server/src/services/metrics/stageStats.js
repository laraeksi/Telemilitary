/**
 * Pseudocode:
 * 1) Compute fail rate, avg time, retries per stage.
 */

export const getStageStats = async (configId) => {
  // TODO: Calculate fail rate, avg time, retries per stage.
  return { configId, stats: [] };
};
