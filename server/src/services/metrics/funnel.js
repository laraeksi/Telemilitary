/**
 * Pseudocode:
 * 1) Aggregate stage_start -> stage_complete per stage.
 * 2) Compute conversion/drop-off.
 */

export const getFunnelMetrics = async (configId) => {
  // TODO: Aggregate funnel metrics from telemetry events.
  return {
    configId,
    stages: [],
  };
};
