/**
 * Pseudocode:
 * 1) Document Stage model shape.
 * 2) Stage config lives in shared/stages.js.
 */

/**
 * @typedef {Object} Stage
 * @property {number} stageId
 * @property {string} name
 * @property {Object} baseParameters
 * @property {"all_pairs_matched"} completionRule
 */

export const STAGE_MODEL_DOCS = {};
