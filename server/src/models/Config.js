/**
 * Pseudocode:
 * 1) Document Config model shape.
 * 2) Store parameter sets per config.
 */

/**
 * @typedef {Object} Config
 * @property {"easy"|"balanced"|"hard"} configId
 * @property {string} label
 * @property {Object} parameterSet
 */

export const CONFIG_MODEL_DOCS = {};
