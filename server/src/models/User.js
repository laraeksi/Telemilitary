/**
 * Pseudocode:
 * 1) Document User model shape.
 * 2) Add database mapping later.
 */

/**
 * @typedef {Object} User
 * @property {string} userId
 * @property {string} createdAt
 * @property {string[]=} segmentTags
 */

export const USER_MODEL_DOCS = {};
